extends layout
block head
    script(src="javascripts/vue.js")
    script(src="javascripts/vue-resource.js")
block content
    p Register
    #form
        label 用户名
            input(v-model="name").xxx
            p {{name}}
        label 密码
            input(v-model="password").password
            p {{password}}

    //#example
        div {{msg}}
    #div(v-demo.literal="foo bar baz") 878789
    script.
        var demo = new Vue({
            el: '#form',
            data: {
             name:"",
             password:""
            },

            methods: {
                greet: function () {
                    console.log(this.name+"x")
                    console.log(this.password)
                    var vm = this;
                    vm.$http.post('/register', {Sa: vm.name,Sb:vm.password}).then(function (response) {
                        alert(response.text());
                    })
                }
            }
        })
    script.
        Vue.directive('demo', function (value) {
            console.log(value) // "foo bar baz"
        })
        var vm = new Vue({
            el:"#div",
            data:{
                
            }
        })